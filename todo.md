- High
    - cross device sync
    - manual time tracking
- low
    - trend explain
    - right click app regex give u the regex that sorted it
    - remove pie chart from calander rightsidebar (add a settings for on or off, default off)
    - speedups
        - detailed /trending
        - calendar
        - regex
- not planned
    - afk detection
    - macos

struct and tables

when we delte anu log

appmetadata key = deleted_log_ids = vec<i64>

we need to store server ip appmatedata
if null then unlocked

we have a list of devices

we will get devices 
